def plus1(number: int) -> int:
    """This function will add 1 to a number and return it

    Args:
        number (int): A number

    Returns:
        int: The original number plus 1
    """
    return number + 1
